# Hosting

Options:

- App Service (deploy zip file)
- Container

Decision: App Service. Reasons:

- We've used this before, super simple to push a zip to App Service

Note: if the app becomes more complex with dependencies we can swap to a container later.
