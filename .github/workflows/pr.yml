name: Pull Request

on:
  pull_request:
    branches:
      - main
      - "feature/**"

permissions:
  contents: read
  checks: write
  pull-requests: read

jobs:
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      docs_site: ${{ steps.filter.outputs.docs_site }}
      migrate_db: ${{ steps.filter.outputs.migrate_db }}
    steps:
      - name: Filter paths
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            docs_site:
              - 'docs-site/**'
            migrate_db:
              - 'migrate-db/**'

  ci-api:
    name: CI API
    uses: ./.github/workflows/node-ci.yml
    with:
      node-version: 22.x
      audit-script: npm run audit
      run-audit: ${{ github.event.pull_request.user.login != 'dependabot[bot]' }}
      pre-test-script: npm run test:ci:start-db
      test-script: npm run test:ci
      output-test-results: true

  ci-migrations-package-run:
    name: CI Migrations Package - Run
    needs: changes
    if: needs.changes.outputs.migrate_db == 'true'
    uses: ./.github/workflows/node-ci.yml
    with:
      node-version: 22.x
      audit-script: npm run audit
      pre-run-script: npm ci --prefix ..
      pre-test-script: npm run test:ci:start-db --prefix ..
      run-audit: ${{ github.event.pull_request.user.login != 'dependabot[bot]' }}
      working-directory: ./migrate-db
      test-script: npm run verify-migrations

  ci-migrations-package:
    name: CI Migrations Package
    needs: [changes, ci-migrations-package-run]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check result
        run: |
          if [[ "${{ needs.changes.outputs.migrate_db }}" != "true" ]]; then
            echo "Skipped — no migrate-db changes"
            exit 0
          fi
          if [[ "${{ needs.ci-migrations-package-run.result }}" == "success" ]]; then
            exit 0
          fi
          echo "CI Migrations Package failed"
          exit 1

  ci-docs-site-run:
    name: CI Docs Site - Run
    needs: changes
    if: needs.changes.outputs.docs_site == 'true'
    uses: ./.github/workflows/node-ci.yml
    with:
      node-version: 22.x
      audit-script: npm run audit
      run-audit: ${{ github.event.pull_request.user.login != 'dependabot[bot]' }}
      working-directory: ./docs-site
      run-build: true
      build-script: npm run build:pr

  ci-docs-site:
    name: CI Docs Site
    needs: [changes, ci-docs-site-run]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check result
        run: |
          if [[ "${{ needs.changes.outputs.docs_site }}" != "true" ]]; then
            echo "Skipped — no docs-site changes"
            exit 0
          fi
          if [[ "${{ needs.ci-docs-site-run.result }}" == "success" ]]; then
            exit 0
          fi
          echo "CI Docs Site failed"
          exit 1
