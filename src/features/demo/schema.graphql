"""
Input to start the onboarding process.
"""
input OnboardingInput {
  email: EmailAddress!
  givenName: String!
  familyName: String!
  kyc: Boolean
}

extend type Mutation {
  beginOnboarding(input: OnboardingInput!): Void
}
