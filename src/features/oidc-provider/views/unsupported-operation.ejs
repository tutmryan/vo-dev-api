<style>
  .error-msg {
    background-color: #f8d7da;
    color: #721c24;
    padding: 10px;
    border-radius: 5px;
    margin: 10px 0;
    font-size: 16px;
    text-align: center;
  }
</style>
<div class="error-msg">
  It looks like there may be an issue with your sign-in setup (OIDC request). This can happen if some details, like the
  client ID, redirect URL, or other settings, don't match what the identity provider expects. Please review your OIDC
  configuration to make sure everything is aligned the requirements.
</div>